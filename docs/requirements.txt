# ReadTheDocs build environment.
#
# The floors match the runtime dependencies in pyproject.toml and the
# ceilings match its majors, so the docs build against the same versions
# the library supports. Pinning `~=` to one minor release here is what let
# this file drift to numpy 1.24 / pandas 2.0 while the library moved on,
# and the autodoc pages then documented a version nobody installs.
numpy>=1.24.3,<3
matplotlib>=3.7.5,<4
pandas>=2.0.3,<4
scipy>=1.10.1,<2
statsmodels>=0.14.1,<0.16
chardet>=5.0.0,<8
setuptools>=78.1.1
sphinx>=4.0.0,<6.0.0
myst-parser>=1.0.0,<4
